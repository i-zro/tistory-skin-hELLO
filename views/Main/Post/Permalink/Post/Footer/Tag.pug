s_tag_label
  #tags(class='mt-2 flex flex-wrap gap-x-1.5 text-[0]')
    | [##_tag_label_rep_##]

style(once='tags')
  :postcss
    #tags {
      a {
        @apply text-sm text-sm rounded-full py-2 px-4 mt-2 bg-b-350 text-b-900;

        &:hover {
          @apply bg-b-purple;
        }

        &:first-of-type::before {
          @apply mr-2 fa-tag;
        }
      }
    }
