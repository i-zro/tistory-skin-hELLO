s_paging
  ul#paging(class='relative z-40 flex justify-center items-center mt-8 lg:mt-12')
    li
      a([##_prev_page_##] class='[##_no_more_prev_##]')
        span(class='text-hidden') 이전
        i(class='fa fa-angle-left')
    s_paging_rep
      li
        a([##_paging_rep_link_##]) [##_paging_rep_link_num_##]
    li
      a([##_next_page_##] class='[##_no_more_next_##]')
        i(class='fa fa-angle-right')
        span(class='text-hidden') 다음

style
  :postcss
    #paging {
      a {
        @apply inline-block;
      }

      span {
        @apply text-sm;
      }

      i,
      span {
        @apply w-8 h-8 leading-8 text-center mx-1 text-base text-h-400 dark:text-b-700;
      }

      span:not(.selected) {
        @apply hidden lg:inline-block;
      }

      .selected {
        @apply rounded-full bg-h-700/60 dark:bg-b-350 inline-block text-h-400 dark:text-b-900;
      }
    }
